import { ImageSrcEmptyHandlerDirective } from './image-src-empty-handler.directive';

describe('ImageSrcEmptyHandlerDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageSrcEmptyHandlerDirective();
    expect(directive).toBeTruthy();
  });
});
