import { ImageLoadErrorHandlerDirective } from './image-load-error-handler.directive';

describe('ImageLoadErrorHandlerDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageLoadErrorHandlerDirective();
    expect(directive).toBeTruthy();
  });
});
